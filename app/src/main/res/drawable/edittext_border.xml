<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/white"/> <!-- Warna background -->
    <stroke android:width="2dp" android:color="@android:color/darker_gray"/> <!-- Ketebalan dan warna border -->
    <corners android:radius="8dp"/> <!-- Membuat sudut agak membulat -->
    <padding android:left="8dp" android:right="8dp" android:top="4dp" android:bottom="4dp"/>
</shape>
